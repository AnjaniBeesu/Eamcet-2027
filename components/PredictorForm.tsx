"use client";
import { useMemo,useState } from "react";
import { ArrowRight,RotateCcw } from "lucide-react";
import { colleges,categories } from "@/data/colleges";
import { predict } from "@/lib/predict";
import type { Category,Gender } from "@/lib/types";
const branches=["ALL","CSE","ECE","EEE","INF","CSM","CSD","CSC"] as const;
export function PredictorForm(){const [rank,setRank]=useState(10000),[gender,setGender]=useState<Gender>("boys"),[category,setCategory]=useState<Category>("OC"),[branch,setBranch]=useState("ALL"),[run,setRun]=useState(false);const results=useMemo(()=>predict(colleges,rank,gender,category,branch),[rank,gender,category,branch]);return <div className="grid gap-0 overflow-hidden rounded-[28px] border border-white/10 bg-[#101010] lg:grid-cols-[380px_1fr]"><div className="border-b border-white/10 p-6 sm:p-9 lg:border-b-0 lg:border-r"><p className="text-xs font-black uppercase tracking-[.2em] text-[#f4c542]">01 / Your profile</p><h2 className="mt-3 text-2xl font-black">Find colleges for your rank.</h2><p className="mt-2 text-sm leading-6 text-zinc-500">Compare against 2025 final-phase closing ranks.</p><div className="mt-7 space-y-4"><Field label="TG EAPCET rank"><input type="number" min={1} value={rank} onChange={e=>setRank(Math.max(1,Number(e.target.value)||1))} className="w-full rounded-xl border border-white/10 bg-black px-4 py-3 outline-none focus:border-[#f4c542]"/></Field><Field label="Category"><select value={category} onChange={e=>setCategory(e.target.value as Category)} className="w-full rounded-xl border border-white/10 bg-black px-4 py-3">{categories.map(x=><option key={x}>{x}</option>)}</select></Field><Field label="Gender"><select value={gender} onChange={e=>setGender(e.target.value as Gender)} className="w-full rounded-xl border border-white/10 bg-black px-4 py-3"><option value="boys">Boys</option><option value="girls">Girls</option></select></Field><Field label="Branch"><select value={branch} onChange={e=>setBranch(e.target.value)} className="w-full rounded-xl border border-white/10 bg-black px-4 py-3">{branches.map(x=><option key={x}>{x}</option>)}</select></Field><div className="flex gap-2"><button onClick={()=>setRun(true)} className="flex flex-1 items-center justify-center gap-2 rounded-xl bg-[#f4c542] px-4 py-3 font-black text-black">Find colleges <ArrowRight size={16}/></button><button onClick={()=>{setRank(10000);setGender("boys");setCategory("OC");setBranch("ALL");setRun(false)}} className="rounded-xl border border-white/10 px-4 text-zinc-400"><RotateCcw size={16}/></button></div></div></div><div className="min-h-[560px] p-6 sm:p-9"><div className="flex items-end justify-between"><div><p className="text-xs font-black uppercase tracking-[.2em] text-zinc-600">02 / Results</p><h3 className="mt-2 text-2xl font-black">{run?`${results.length} options found`:"Your shortlist"}</h3></div>{run&&<span className="rounded-full border border-white/10 px-3 py-1 text-xs text-zinc-500">2025 final</span>}</div>{!run?<div className="mt-8 grid h-[430px] place-items-center rounded-2xl border border-dashed border-white/10 text-center"><div><div className="mx-auto mb-4 text-4xl">⌁</div><p className="font-bold">Enter your details to begin.</p><p className="mt-2 max-w-xs text-sm text-zinc-500">Use your rank, category, gender and branch to build a practical shortlist.</p></div></div>:<div className="mt-6 space-y-3">{results.slice(0,15).map(r=><article key={`${r.college.code}-${r.courseCode}`} className="rounded-2xl border border-white/8 bg-black/30 p-4"><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap gap-2"><b>{r.college.name}</b><span className="rounded-full bg-white/5 px-2 py-1 text-[10px] font-black uppercase text-zinc-400">{r.tier}</span></div><p className="mt-1 text-xs text-zinc-500">{r.courseName} · {r.college.place}</p></div><div className="text-right"><p className="text-[10px] text-zinc-600">CLOSING</p><b>{r.closingRank.toLocaleString()}</b></div></div><div className="mt-3 h-1 overflow-hidden rounded bg-white/5"><div className="h-full rounded bg-[#f4c542]" style={{width:`${Math.max(10,r.score)}%`}}/></div></article>)}{results.length===0&&<p className="rounded-xl border border-white/10 p-8 text-center text-zinc-500">No matching historical rows. Try another branch or profile.</p>}</div>}</div></div>}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="block"><span className="mb-2 block text-xs font-bold text-zinc-400">{label}</span>{children}</label>}
