"use client";
import { useMemo, useState } from "react";
import { colleges } from "@/data/colleges";
const branches=["ALL","CSE","ECE","EEE","INF","CSM","CSD","CSC"];
export default function CutoffsPage(){const [branch,setBranch]=useState("ALL");const [q,setQ]=useState("");const rows=useMemo(()=>colleges.flatMap(c=>c.courses.map(x=>({college:c,course:x}))).filter(r=>(branch==="ALL"||r.course.code===branch)&&`${r.college.name} ${r.college.code}`.toLowerCase().includes(q.toLowerCase())).sort((a,b)=>(a.course.cutoff.OC.boys??999999)-(b.course.cutoff.OC.boys??999999)),[branch,q]);return <main className="min-h-screen bg-[#070707]"><header className="border-b border-white/8 bg-[#0a0a0a]"><div className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="text-xs font-bold uppercase tracking-[.2em] text-[#f4c542]">Cutoffs / Final phase</p><h1 className="mt-3 text-4xl font-black sm:text-6xl">2025 closing ranks.</h1><p className="mt-4 max-w-2xl text-zinc-500">Historical final-phase closing ranks from the official statement used by this project. Filter the rows to compare branches quickly.</p><div className="mt-8 flex flex-wrap gap-2">{branches.map(b=><button key={b} onClick={()=>setBranch(b)} className={`rounded-full px-4 py-2 text-xs font-black ${branch===b?"bg-[#f4c542] text-black":"border border-white/10 text-zinc-500"}`}>{b}</button>)}</div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search college or code" className="mt-4 w-full rounded-2xl border border-white/10 bg-black px-4 py-3 text-sm outline-none focus:border-[#f4c542]"/></div></header><section className="mx-auto max-w-7xl px-5 py-8 lg:px-8"><div className="overflow-hidden rounded-3xl border border-white/8"><div className="grid grid-cols-[1fr_100px_130px_130px] gap-4 bg-white/[.03] px-5 py-4 text-[10px] font-black uppercase tracking-wider text-zinc-600"><span>College</span><span>Branch</span><span>OC boys</span><span>OC girls</span></div>{rows.map(r=><div key={`${r.college.code}-${r.course.code}`} className="grid grid-cols-[1fr_100px_130px_130px] gap-4 border-t border-white/6 px-5 py-4 text-sm"><div><p className="font-bold">{r.college.name}</p><p className="mt-1 text-xs text-zinc-600">{r.college.code} · {r.college.place}</p></div><span className="font-black text-[#f4c542]">{r.course.code}</span><span>{r.course.cutoff.OC.boys?.toLocaleString()??"—"}</span><span>{r.course.cutoff.OC.girls?.toLocaleString()??"—"}</span></div>)}{rows.length===0&&<p className="p-10 text-center text-zinc-500">No matching rows.</p>}</div></section></main>}